import angular from 'angular';
import 'angular-mocks';

import Controller from '../coach';

describe('Coach Controller', function(){

  // // Start by creating an instance of the controller
  // // that we can use for all subsequent tests.
  // let controller, filter;
  // // Note the use of 'inject' here. That's so we can 'inject' dependencies!
  // beforeEach(inject(function(_$filter_){
  //   // dependencies used by controller
  //   filter = _$filter_;
  //   // OK, create the controller
  //   controller = new Controller(filter);
  // }));

  // it('should filter an array', () => {
  //   expect(controller.filter(['foo','bar','baz'], 'ba')).toEqual(['bar', 'baz']);
  // });

  // it('should have something on the scope', () => {
  //   expect(controller.scopeVar).toEqual('Two-way binding');
  // });


});
